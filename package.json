{
  "name": "wme-place-harmonizer-row-data",
  "version": "0.1.0",
  "license": "GPL-3.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:sdk-values": "node scripts/generate-sdk-values.mjs",
    "validate": "node scripts/validate-data.mjs",
    "config:excel:export": "python scripts/config-excel.py export",
    "config:excel:import": "python scripts/config-excel.py import",
    "config:excel:roundtrip": "python scripts/config-excel.py roundtrip"
  },
  "dependencies": {
    "ajv": "^8.18.0"
  }
}
